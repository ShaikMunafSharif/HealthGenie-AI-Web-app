[pytest]
addopts = -v --tb=short --html=qa_automation/reports/test_report.html --self-contained-html
markers =
    auth: Authentication tests
    navigation: Navigation tests
    forms: Form validation tests
    ui: UI functional tests
    security: Security-oriented tests
    cross_browser: Cross-browser tests
testpaths = qa_automation/tests
